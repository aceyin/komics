# sql配置文件
query.user.and.user_profile.by.username.and.password: >
  select
  u.id,u.username,u.email,u.mobile,u.status,
  p.id `p.id`,p.name,p.gender,p.age
  from user u join user_profile p on u.id = p.user_id